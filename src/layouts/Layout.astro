---
import "@styles/main.css";
import "@styles/global.css";
import Header from "@components/Header.astro";
import Footer from "@components/Footer.astro";
import Popover from "@components/Popover.astro";
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <!-- font -->
     <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inknut+Antiqua:wght@300;400;500;600;700;800;900&family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&family=Montagu+Slab:opsz,wght@16..144,100..700&family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">
    <title>DIN TITEL HER. MÅSKE SOM PROP?</title>
    <style is:inline>
      @layer reset, global, components, overrides;
    </style>
  </head>
  <body>
    <Header />
    <slot />
    <Footer />
    <Popover id="login-popover" title="Dashboard Login" />
  </body>
</html>

<style></style>

<script>
  // open / close / toggle by data attributes
  document.addEventListener('click', (e) => {
    const btn = e.target.closest('[data-popover-target]');
    if (!btn) return;
    const id = btn.dataset.popoverTarget;
    const el = document.getElementById(id);
    if (!el) return;
    const action = btn.dataset.popoverAction || 'open';
    if (action === 'toggle') el.hidden = !el.hidden;
    else if (action === 'open') el.hidden = false;
    else if (action === 'close') el.hidden = true;
  });

  // click backdrop to close
  document.addEventListener('click', (e) => {
    const overlay = e.target.closest('.popover-overlay');
    if (overlay && e.target === overlay) overlay.hidden = true;
  });

  // ESC to close
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      document.querySelectorAll('.popover-overlay:not([hidden])').forEach(el => el.hidden = true);
    }
  });
</script>
